# WebSocket URL used by `pnpm dev` (Vite) to reach the Rust bridge.
# The Rust binary binds 127.0.0.1 and prints its URL to stdout.
# For a reproducible dev loop, run the binary with `RADAR_UI_PORT=43510`
# and set this to `ws://127.0.0.1:43510/ws`.
VITE_WS_URL=ws://127.0.0.1:43510/ws